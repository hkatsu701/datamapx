version: 1

project:
  name: merge_wizard_example
  description: Merge users and accounts into one staging CSV

inputs:
  users:
    path: ./input/users.csv
    encoding: utf-8-sig
    delimiter: ","
    header: true
    key: id
    schema:
      id:
        type: string
        required: true
        normalize: [trim]
      name:
        type: string
        required: false
        normalize: [trim]
      amount:
        type: decimal
        required: false
        normalize: [trim]

  accounts:
    path: ./input/accounts.csv
    encoding: utf-8-sig
    delimiter: ","
    header: true
    key: id
    schema:
      id:
        type: string
        required: true
        normalize: [trim]
      account_name:
        type: string
        required: false
        normalize: [trim]
      amount:
        type: decimal
        required: false
        normalize: [trim]
      department_name:
        type: string
        required: false
        normalize: [trim]

merge:
  base: users
  join_type: left
  columns:
    id:
      source: users.id
    display_name:
      first:
        - accounts.account_name
        - users.name
    total_amount:
      sum:
        - users.amount
        - accounts.amount
    department_name:
      source: accounts.department_name

output:
  path: ./output/merged.csv
  encoding: utf-8-sig
  delimiter: ","
  header: true
  newline: "\n"
  if_exists: overwrite
  columns:
    - id
    - display_name
    - total_amount
    - department_name

error_handling:
  on_validation_error: output_error
  on_lookup_missing: output_error
  on_transform_error: output_error
  max_errors: 1000
  error_output: ./reports/errors.csv
  skipped_output: ./reports/skipped.csv
  include_original_row: true

runtime:
  run_id: auto
  log_dir: ./logs
  log_level: INFO
  summary_output: ./reports/summary.json

