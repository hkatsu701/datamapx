version: 1

project:
  name: union_example
  description: Append two same-format CSVs into one union CSV

inputs:
  file_a:
    path: ./input/file_a.csv
    encoding: utf-8-sig
    delimiter: ","
    header: true
    key: id
    schema:
      id:
        type: string
        required: true
      name:
        type: string
        required: true
      amount:
        type: decimal
  file_b:
    path: ./input/file_b.csv
    encoding: utf-8-sig
    delimiter: ","
    header: true
    key: id
    schema:
      id:
        type: string
        required: true
      name:
        type: string
        required: true
      amount:
        type: decimal
union:
  columns: [id, name, amount]
output:
  path: ./output/unioned.csv
  encoding: utf-8-sig
  delimiter: ","
  header: true
  newline: "\n"
  if_exists: overwrite
  columns: [id, name, amount]
error_handling:
  on_validation_error: output_error
  on_lookup_missing: output_error
  on_transform_error: output_error
  max_errors: 1000
  error_output: ./reports/errors.csv
  skipped_output: ./reports/skipped.csv
  include_original_row: true
runtime:
  run_id: auto
  log_dir: ./logs
  log_level: INFO
  summary_output: ./reports/summary.json
