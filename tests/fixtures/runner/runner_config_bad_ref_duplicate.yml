version: 1

project:
  name: runner_bad_ref_duplicate

inputs:
  users:
    path: ./input_users.csv
    schema:
      user_id:
        type: string
        required: true
      department_code:
        type: string

references:
  departments:
    path: ./ref_departments_duplicate.csv
    key: department_code
    on_duplicate: error

outputs:
  users_out:
    path: ./output/users.csv
    columns:
      - id
      - department_name

mappings:
  users_out:
    id:
      source: users.user_id
    department_name:
      lookup:
        reference: departments
        key: users.department_code
        value: department_name

error_handling:
  error_output: ./output/errors.csv
  skipped_output: ./output/skipped.csv
